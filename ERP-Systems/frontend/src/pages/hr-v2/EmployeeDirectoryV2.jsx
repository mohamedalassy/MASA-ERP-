import{useEffect,useState}from"react";import{hrApi}from"./hrApi";import"./hr-v2.css";export default function EmployeeDirectoryV2({onNavigate}){const[rows,setRows]=useState([]),[q,setQ]=useState("");useEffect(()=>{const t=setTimeout(()=>hrApi(`/hr/employees?per_page=100&search=${encodeURIComponent(q)}`).then(x=>setRows(x.data||[])),250);return()=>clearTimeout(t)},[q]);return <section className="pv2-page" dir="rtl"><header className="pv2-hero"><div><small>PEOPLE DIRECTORY</small><h1>الموظفون</h1><p>الدليل المركزي وبوابة Employee 360.</p></div></header><input className="pv2-search" value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث عن موظف..."/><article className="pv2-panel">{rows.map(e=><button className="employee-row" key={e.id} onClick={()=>onNavigate?.("hr-v2-employee-360",{employeeId:e.id})}><b>{e.full_name||`${e.first_name||""} ${e.last_name||""}`}</b><span>{e.job_title?.name||"—"}</span><span>{e.department?.name||"—"}</span></button>)}</article></section>}